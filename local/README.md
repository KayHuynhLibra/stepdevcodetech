# local/ — máy bạn only (ngoài be / fe / studying)

Không deploy. Không nhầm với app runtime. **Không giữ bản snapshot version cũ** — source of truth = `be/` + `fe/` hiện tại.

| Path | Nội dung |
|------|----------|
| `hackmyapp/` | Lab kiểm thử phòng thủ (chỉ `README.md` được push) |
| `cybersecurity/` | Ghi chú threat model nội bộ |
| `GitNexus/` · `Grapuco/` | Tool code-intel local |

App chạy: **`be/`** + **`fe/`**. Học tập: **`studying/`**.
